thingsboard:
    enabled: false
    host: tb.server.com
    protocol: https
    port: 443
    access_token: 

deployment:
    lat: 0.0  # REDACTED - see live config on Pi 
    lon: 0.0  # REDACTED - see live config on Pi
    bearing: n

sensors:
    cameras:
        picam_h264:
            enabled: true
            camera_radar: TM_RADAR_SERIAL_PORT_00
        picam_gs_h264:
            enabled: false

    radars:
        TM_RADAR_SERIAL_PORT_00:
            enabled: true

    airquality_monitors:
        sensorname01:
            enabled: false
            mqtt_topic_incoming: readings

time:
    timezone: America/Los_Angeles
    npt_set: true

addons:
    adafruit_io:
        username: "targadriver"
        key: "REDACTED_SET_LOCALLY_IN_opt_traffic-monitor"
        feed: "radar-speed"
